la solution est:
IF (Sex=Low) and ( Balance>=0.82 ) THEN ( Inconsistent spacing )
  IF (Sex=Meduim) and ( Grouping<=0.29 ) THEN ( Screen clutter )
  IF (UX=High) and ( Symmetry>=0.82 ) THEN ( Incorrect appearance of widgets )
  IF (Sex=High) and ( Color count>=0.15 ) THEN ( Inconsistent spacing )
  IF (Education Level=Low) and ( Balance<=0.64 ) THEN ( Expected information missing )
  IF (Age=Low) and ( Link count>=0.42 ) THEN ( Screen clutter )
  IF (Age=Meduim) and ( Layout uniformity>=0.91 ) THEN ( Home page is too busy )
  IF (Age=High) and ( Graphics Count>=0.37 ) THEN ( Information overload )
  IF (Sex=Meduim) and ( Economy<=0.67 ) THEN ( Expected information missing )
  IF (Education Level=High) and ( Graphics Count<=0.0 ) THEN ( Functionality unclear )
  IF (Education Level=High) and ( Graphics Count<=0.59 ) THEN ( Deficient content inside the "about us" tab )
  IF (Sex=Meduim) and ( Cohesion<=0.21 ) THEN ( Incorrect appearance of widgets )
  IF (UX=Meduim) and ( Homogeneity>=0.8 ) THEN ( Functionality unclear )
  IF (Sex=High) and ( Word count>=0.67 ) THEN ( Missing functionality )
  IF (Age=High) and ( Link count>=0.12 ) THEN ( Incorrect appearance of widgets )
  IF (Sex=Low) and ( Balance<=0.63 ) THEN ( Incorrect appearance of widgets )
  IF (Age=High) and ( Unity>=0.3 ) THEN ( Terminology unclear )
  IF (UX=High) and ( Economy<=0.8 ) THEN ( Deficient content inside the "about us" tab )
  IF (Sex=High) and ( Unity<=0.13 ) THEN ( Missing functionality )
  IF (Education Level=High) and ( Symmetry>=0.65 ) THEN ( Deficient content inside the "about us" tab )
  IF (Age=Meduim) and ( Grouping<=0.98 ) THEN ( Lack of task support )
  IF (Education Level=High) and ( Fonts count<=0.35 ) THEN ( Incomprehensible instructions )
  IF (UX=Low) and ( Sequence<=0.72 ) THEN ( Incomprehensible instructions )
  IF (Age=Low) and ( Economy<=0.49 ) THEN ( Functionality unclear )
  IF (Age=Low) and ( Word count<=0.0 ) THEN ( Expected information missing )
  IF (Sex=Meduim) and ( Sequence>=0.03 ) THEN ( Information overload )
  IF (Age=High) and ( Graphics Count>=0.05 ) THEN ( Missing functionality )
  IF (Sex=High) and ( Layout uniformity<=0.67 ) THEN ( Incorrect layout of widgets )
  IF (UX=Low) and ( Fonts count<=0.9 ) THEN ( Terminology unclear )
  IF (Age=Meduim) and ( Layout Complexity<=0.33 ) THEN ( Screen clutter )
  IF (UX=Meduim) and ( Layout uniformity>=0.82 ) THEN ( Functionality unclear )
  IF (Age=Meduim) and ( Cohesion>=0.13 ) THEN ( Expected information missing )
  IF (Sex=Meduim) and ( Layout Complexity<=0.72 ) THEN ( Incomprehensible instructions )
  IF (UX=Low) and ( Layout uniformity<=0.77 ) THEN ( Terminology unclear )
  IF (Sex=Low) and ( Economy<=0.27 ) THEN ( Incomprehensible instructions )
  IF (UX=Low) and ( Regularity<=0.02 ) THEN ( Incorrect appearance of widgets )
  IF (Education Level=Low) and ( Sequence<=0.24 ) THEN ( Missing functionality )
  IF (UX=High) and ( Graphics Count>=0.79 ) THEN ( Screen clutter )
  IF (Age=Meduim) and ( Homogeneity<=0.1 ) THEN ( Missing functionality )
  IF (UX=High) and ( Word count>=0.13 ) THEN ( Home page is too busy )
  IF (Sex=High) and ( Unity>=0.72 ) THEN ( Missing functionality )
  IF (UX=Meduim) and ( Economy<=0.05 ) THEN ( Deficient content inside the "about us" tab )
  IF (Age=Meduim) and ( Color count>=0.1 ) THEN ( Terminology unclear )
  IF (Education Level=Meduim) and ( Sequence<=0.88 ) THEN ( Missing functionality )
  IF (UX=Meduim) and ( Simplicity<=0.68 ) THEN ( Incorrect layout of widgets )
  IF (UX=High) and ( Link count<=0.2 ) THEN ( Missing functionality )
  IF (Education Level=Meduim) and ( Cohesion<=0.95 ) THEN ( Home page is too busy )
  IF (Age=Low) and ( Layout uniformity<=0.04 ) THEN ( Expected information missing )
  IF (Sex=Low) and ( Grouping>=0.37 ) THEN ( Terminology unclear )
  IF (Education Level=Meduim) and ( Graphics Count>=0.78 ) THEN ( Incomprehensible instructions )
  
